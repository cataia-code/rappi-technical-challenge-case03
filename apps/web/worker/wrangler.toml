name = "rappi-caso03-llm-proxy"
main = "index.js"
compatibility_date = "2026-07-30"
workers_dev = true
preview_urls = false

# Non-secret config can go here as [vars]; API keys are set with `wrangler secret put`
# and must never be committed. See index.js header comment for deploy steps.
[vars]
LLM_PROVIDER_ORDER = "groq,gemini,openrouter"
