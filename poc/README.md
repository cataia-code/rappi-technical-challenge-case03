# Proofs of Concept

This directory is reserved for isolated prototypes that should not be part of the
production decision engine yet.

Current production surfaces live in:

- `src/caso03/` for the decision engine.
- `apps/web/` for the static dashboard.
- `apps/api/` for the planned FastAPI surface.
- `apps/mcp/` for the MCP server.
